# نظام فواتير المياه - Flask

مشروع جاهز لإدارة:
- تسجيل الدخول الآمن
- المشتركين
- الفواتير والقراءات
- السداد والمتأخرات
- المرفقات والصور
- التقارير
- التصدير PDF
- الاستيراد من Excel / XML / CSV
- إرسال واتساب عبر API

## بيانات الدخول الافتراضية
- المستخدم: 
- كلمة المرور: 

## التشغيل
```bash
pip install -r requirements.txt
python run.py
```

ثم افتح:
```text
http://127.0.0.1:5000
```

## ملاحظات
- قاعدة البيانات SQLite تُنشأ تلقائياً عند أول تشغيل داخل `instance/`
- يمكن تغيير اسم المشروع واسم المنظمة واسم المهندس من صفحة الإعدادات
- يمكن تعديل سعر الوحدة ورسوم الاشتراك لكل مشترك أو من الإعدادات العامة
- يمكن تصدير الفاتورة كـ PDF وفتحها للطباعة
- يدعم الاستيراد من ملفات Excel وXML وCSV

## الملفات المهمة
- `app.py` التطبيق الرئيسي
- `templates/` واجهات HTML
- `uploads/` المرفقات
- `instance/` قاعدة البيانات



## جديد
- شجرة حسابات محاسبية
- لوحة موظف
- طبقة SQLAlchemy للنماذج الجديدة
